import { Clock, Receipt, CurrencyDollar, ClockCounterClockwise, CheckCircle, Warning, Notepad, Download, ArrowUp, ArrowDown } from '@phosphor-icons/react'
import type React from 'react'

export const ICON_MAP: Record<string, React.ComponentType<any>> = {
  Clock, Receipt, CurrencyDollar, ClockCounterClockwise, CheckCircle, Warning, Notepad, Download, ArrowUp, ArrowDown,
}
